#!/usr/bin/expect -f

set timeout 30
set host "72.61.229.171"
set user "root"
set password ")vbofQi/lHXSMqF?Nk8E"

spawn ssh -o StrictHostKeyChecking=no $user@$host

expect {
    "password:" {
        send "$password\r"
    }
}
expect "#"

send "cat /etc/nginx/sites-available/ibaadattracker.in\r"
expect "#"

send "ls /var/www/ibaadattracker.in/\r"
expect "#"

send "exit\r"
expect eof
