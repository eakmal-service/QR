#!/usr/bin/expect -f

set timeout 30
set host "72.61.229.171"
set user "root"
set password ")vbofQi/lHXSMqF?Nk8E"

spawn ssh -o StrictHostKeyChecking=no $user@$host

expect {
    "password:" {
        send "$password\r"
    }
}
expect "#"

send "echo '--- QR CONFIG ---'\r"
send "cat /etc/nginx/sites-enabled/qr.akmal.in\r"
expect "#"

send "echo '--- NPLUSONE CONFIG ---'\r"
send "cat /etc/nginx/sites-enabled/nplusonefashion.com\r"
expect "#"

send "exit\r"
expect eof
