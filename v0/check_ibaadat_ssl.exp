#!/usr/bin/expect -f

set timeout 30
set host "72.61.229.171"
set user "root"
set password ")vbofQi/lHXSMqF?Nk8E"

spawn ssh -o StrictHostKeyChecking=no $user@$host

expect {
    "password:" {
        send "$password\r"
    }
}
expect "#"

# Check if SSL cert exists for ibaadattracker.in
send "ls /etc/letsencrypt/live/ | grep ibaadat\r"
expect "#"

send "exit\r"
expect eof
